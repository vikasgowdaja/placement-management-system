package New2;
//Java Program to remove all the whitespace from a string
import java.util.*;
public class Main
{
	if(firstName&&lastNAme==null){
		  throw new MyException(ErrorEnum.OBJECT_NULL)
	}
	
		return firstName
				;
	
   

public static void main(String []args)
{
    //Take input from the user
   Scanner sc=new Scanner(System.in);
  
   String firstName=sc.nextLine();
   String lastName=sc.nextLine();//Initialize the String
   
   firstName = firstName.replaceAll("\\s", ""); 
   lastName = lastName.replaceAll("\\s", ""); 
   System.out.println("After Removing Whitespaces the entered string is:"+firstName+lastName); 
}
}